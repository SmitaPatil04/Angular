import { HightightDirective } from './hightight.directive';

describe('HightightDirective', () => {
  it('should create an instance', () => {
    const directive = new HightightDirective();
    expect(directive).toBeTruthy();
  });
});
